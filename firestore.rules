rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Chacun ne peut lire/écrire que son propre document, jamais celui d'un autre.
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
